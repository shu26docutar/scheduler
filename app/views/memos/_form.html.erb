
<div class="main-memo">
  <%= form_with model: [@room, @memo], class: 'form', local: true do |f| %>
    <div class="form-input">
      <%= f.text_field :content, class: 'form-memo', placeholder: 'メモの入力' %>
    </div>
      <%= f.submit '保存', class: 'form-submit' %>
  <% end %>
</div>