<div id="schedule-modal">
<%= form_with(odel: @event, id: "schedule-modal", local: true) do |f| %>

    <div class="schedule_title">
        <%= f.text_field :title, class: 'form-event', id: 'form-event', placeholder: 'Title' %>
    </div>

    <div class="schedule_time">
        <%= f.text_field :first_time, class: 'form_time', id: 'first-show', placeholder: 'times' %>
    </div>

    <div class="submit">
        <%= f.submit '保存'%>
    </div>
<% end %> 
</div>